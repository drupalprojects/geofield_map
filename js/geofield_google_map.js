(function ($, Drupal, drupalSettings) {

  'use strict';

  Drupal.behaviors.geofieldGoogleMapInit = {
    attach: function (context, drupalSettings) {

    }
  };

  Drupal.geofieldGoogleMap = {

  };

})(jQuery, Drupal, drupalSettings);
